/** The "threefold" identity mark — three ascending bars (Ternus = three). */
export function Mark() {
  return (
    <span className="mark" aria-hidden>
      <i />
      <i />
      <i />
    </span>
  );
}
